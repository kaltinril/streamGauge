import numpy as np
import hog_generator as hg


def train(data_loc):
    # retrieve data from files
    all_data = hg.load_hogs(data_loc)
    # split data to training and validation. Just random, no boosting for now. 80/20 rule

    # train the ANN

    # save the weights and statistics
    raise NotImplementedError


def test(weights_loc, img):
    # load the weights and prepare objects
    # classify pixels in image
    raise NotImplementedError


if __name__ == '__main__':
    raise NotImplementedError